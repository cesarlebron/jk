node_modules/
package-lock.json
.DS_Store

# gstack per-repo runtime state (daemon pid/port, session tokens, browse logs)
.gstack/
.gstack-worktrees/

# graphify: grafo generado desde el código, se reconstruye con `graphify update .`
graphify-out/
